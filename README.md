# shortcuts

    /t = harvest{t:#s.nuutec.pub_info}
    /kh = kernel.hardline
    /u = sys.upgrades
    /lock = mode{m:"lock"}
    /dev = mode{m:"dev"}
    /dc = kernel.hardline {dc:true}

# safe targets

- nuutec
